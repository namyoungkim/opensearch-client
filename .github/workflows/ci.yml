name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python
        run: uv python install 3.11

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Ruff check
        run: uv run ruff check .

      - name: Ruff format check
        run: uv run ruff format --check .

      - name: Type check
        run: uv run ty check

  test:
    runs-on: ubuntu-latest
    needs: lint
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Run unit tests with coverage
        run: uv run pytest tests/unit -v --cov=opensearch_client --cov-report=term-missing --cov-fail-under=70

  integration:
    runs-on: ubuntu-latest
    needs: test

    steps:
      - uses: actions/checkout@v4

      - name: Start OpenSearch with Nori plugin
        run: docker compose -f docker-compose.test.yml up -d

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python
        run: uv python install 3.11

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Wait for OpenSearch
        run: |
          for i in {1..30}; do
            curl -s http://localhost:9201 && break
            echo "Waiting for OpenSearch..."
            sleep 2
          done

      - name: Run integration tests
        env:
          OPENSEARCH_TEST_HOST: localhost
          OPENSEARCH_TEST_PORT: 9201
        run: uv run pytest tests/integration -v

      - name: Stop OpenSearch
        if: always()
        run: docker compose -f docker-compose.test.yml down
